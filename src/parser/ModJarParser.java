package parser;

public class ModJarParser {
	//AFTER we set the version from the website, we also need to load it from the jar, see if it looks like a valid version
	//and if so, overwrite the website version with the one in the jar.
}